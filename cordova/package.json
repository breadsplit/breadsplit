{
  "name": "com.github.breadsplit",
  "displayName": "BreadSplit",
  "version": "1.0.0",
  "scripts": {
    "check": "cordova requirements",
    "prepare": "cordova prepare",
    "clean": "rimraf ./platforms ./plugins ./www",
    "build": "cordova build",
    "android:build": "cordova build android",
    "android:run": "cordova run android",
    "android:emu": "cordova emulate android"
  },
  "dependencies": {
    "cordova-android": "^8.0.0"
  },
  "devDependencies": {
    "cordova": "^9.0.0",
    "cordova-plugin-whitelist": "^1.3.3",
    "rimraf": "^2.6.3"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {}
    },
    "platforms": [
      "android"
    ]
  }
}